<nav class="navbar" role="navigation" aria-label="main navigation">
  <div class="navbar-brand ml-3">
    <%= link_to root_path do %>
      <%= image_tag "logo.png", size: "65x65" %>
    <% end %>
  </div>

    <div class="navbar-end">
      <div class="navbar-menu mr-5">
        <div class="buttons">
          <% unless current_page?(root_path)||current_page?(new_board_path) %>
            <%= link_to t("boards.new.title"), new_board_path, class: "button" %>
          <% end %>

          <% unless current_page?(root_path)||current_page?(boards_path) %>
            <%= link_to t("boards.index.title"), boards_path, class: "button" %>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</nav>